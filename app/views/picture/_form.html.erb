<%= form_with(model: [imageable, Picture.new], local: true) do |form| %>
  <div class="field">
    <%= form.label :image %><br/>
    <%= form.file_field :avatar %>
  </div>
  <%= form.submit class: "btn btn-primary" %>
<% end %>
